#include <iostream>

using namespace std;

int main(){
	double A,B;
	cin>>A>>B;
  /*c++에서 소수점 자리 표현하는 방법*/
  //소수점 제일 아래에서 반올림되는 것 해제해줌
	cout<<fixed;
  //9자리까지 소수점 아래 표현하겠음.
	cout.precision(10);
//	cout.unsetf(ios::fixed);
	cout<<(A/B);
	return 0;
}
