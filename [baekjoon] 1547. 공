#include <iostream>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	int M,X,Y;
	int ball=1;
	cin>>M;
	for(int i=0;i<M;i++){
		cin>>X>>Y;
		if(X==ball) ball=Y;
		else if(Y==ball) ball=X;
	}
	cout<<ball;
	return 0;
}
