#include <iostream>

using namespace std;

int main(){
	int N,num,sum=0,maxval=-987987987;
	
	cin>>N;
	for(int i=0;i<N;i++){
		cin>>num;
		sum+=num;
		if(sum>maxval) maxval = sum;
		if(sum<0) sum=0;
	}
	
	cout<<maxval;
	return 0;
}
