#include <iostream>
#include <string>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	string S;
	
	cin>>S;
	for(int i=0;i<S.size()/2;i++){
		if(S[i]!=S[S.size()-1-i]){
			cout<<'0';
			return 0;
		}
	}

	cout<<'1';

	return 0;
}
